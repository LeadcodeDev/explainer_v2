---
import { CodeGroup as CodeGroupReact } from './code-group.tsx'

interface Props {
  titles?: string[]
  className?: string
}

const { titles, className } = Astro.props
---

<CodeGroupReact client:load titles={titles} className={className}>
  <slot />
</CodeGroupReact>
